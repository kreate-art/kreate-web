/**
 * `ProjectBenefitsEditor` is the editor of `ProjectBenefits`.
 *
 * It receives `value: ProjectBenefits`,
 * triggers `onChange(newValue)` on every change.
 */

export default function ProjectBenefitsEditor() {
  return null;
}
