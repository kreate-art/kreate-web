export type Point = { x: number; y: number };
export type RelativePoint = Point;
export type AbsolutePoint = Point;

export type Size = { w: number; h: number }; // absolute
export type AspectRatio = number; // absolute
export type Area = number; // absolute

export type Rect = { x: number; y: number; w: number; h: number };
export type RelativeRect = Rect;
export type AbsoluteRect = Rect;
